import type { CardDef, CardCategory } from './game';

export type { CardDef, CardCategory };

// ─── Mazo (deck) ────────────────────────────────────────────────

export type Deck = CardDef[];

// Phase 2: 52-card deck composition data (CargaKi×15, Esquive×4, etc.)
